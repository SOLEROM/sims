# fg-demo profile defaults (sourced by simrun.sh after env.list, before local.env)

# anchor host-port map: internal ports identical on every vehicle, per-ID only
# at the host edge (%ID% is replaced by the vehicle id — supports ID 1..9 here)
AGENT_PUBLISH="1577%ID%:15777 1578%ID%:15778/udp"
